services:
  paisa:
    image: ananthakumaran/paisa:latest
    ports:
      - 31203:7500
    volumes:
      - "${CONFIG_DIR}/paisa:/root/Documents/paisa"
    restart: unless-stopped
    networks:
      - proxy
  paperless-redis:
    image: docker.io/library/redis:8
    container_name: paperless-redis
    restart: unless-stopped
    networks:
      - proxy
    volumes:
      - "${CONFIG_DIR}/paperless/redis:/data"
  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: paperless-ngx
    restart: unless-stopped
    depends_on:
      - paperless-redis
    ports:
      - "31204:8000"
    networks:
      - proxy
    volumes:
      - "${MEDIA_DIR}/docs:/usr/src/paperless/media"
      - "${CONFIG_DIR}/paperless:/usr/src/paperless/data"
      - "${CONFIG_DIR}/paperless/export:/usr/src/paperless/export"
      - "${CONFIG_DIR}/paperless/consume:/usr/src/paperless/consume"
    #env_file: docker-compose.env
    environment:
      PAPERLESS_REDIS: redis://paperless-redis:6379
  quartz:
    image: shommey/dockerized-quartz
    container_name: quartz
    ports:
      - "31201:8080"
      - "31251:3001"
    volumes:
      - ${MEDIA_DIR}/notes:/usr/src/app/content
    restart: unless-stopped
    networks:
      - proxy
  vikunja:
    image: vikunja/vikunja
    container_name: vikunja
    ports:
      - 31202:3456
    volumes:
      - "${CONFIG_DIR}/vikunja/files:/app/vikunja/files"
    environment:
      VIKUNJA_DATABASE_HOST: vikunja-db
      VIKUNJA_DATABASE_PASSWORD: changeme
      VIKUNJA_DATABASE_TYPE: mysql
      VIKUNJA_DATABASE_USER: vikunja
      VIKUNJA_DATABASE_DATABASE: vikunja
      VIKUNJA_SERVICE_JWTSECRET: supersecret
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - proxy
  db:
    image: mariadb:10
    container_name: vikunja-db
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - "${CONFIG_DIR}/vikunja/db:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: supersecret
      MYSQL_USER: vikunja
      MYSQL_PASSWORD: changeme
      MYSQL_DATABASE: vikunja
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u $$MYSQL_USER --password=$$MYSQL_PASSWORD"]
      interval: 2s
      start_period: 30s
    restart: unless-stopped
    networks:
      - proxy
  #zen:
  #  image: lscr.io/linuxserver/zen:latest
  #  container_name: zen-browser
  #  security_opt:
  #    - seccomp:unconfined #optional
  #  environment:
  #    - PUID=1000
  #    - PGID=1000
  #    - TZ=Asia/Kolkata
  #  volumes:
  #    - "${CONFIG_DIR}/zen:/config"
  #  ports:
  #    - 3000:3000
  #    - 31204:3001
  #  shm_size: "1gb"
  #  restart: unless-stopped
